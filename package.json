{
  "scripts": {
      "server": "cd server && npm run build && node dist/server/seeds/index.js && node dist/server/server.js",
      "server:no-seed": "cd server && npm run start"
  },
  "devDependencies": {
    "vite": "^6.2.4"
  }
}
